<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="12dp"
    android:background="?android:attr/selectableItemBackground"
    android:gravity="center_vertical">

    <!-- Replace the private system drawable with a simple shape or text -->
    <TextView
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:text="→"
        android:textSize="16sp"
        android:textColor="@color/text_secondary"
        android:gravity="center"
        android:layout_marginEnd="12dp" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvStudentName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Student Name"
            android:textSize="14sp"
            android:textColor="@color/text_primary" />

        <TextView
            android:id="@+id/tvCourseTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Course Title"
            android:textSize="12sp"
            android:textColor="@color/text_secondary"
            android:layout_marginTop="2dp" />

    </LinearLayout>

    <!-- Replace the private system drawable with Material icon or remove -->
    <Button
        android:id="@+id/btnDeleteEnrollment"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:text="×"
        android:textSize="18sp"
        android:textColor="@color/white"
        android:background="@color/accent_color"
        android:gravity="center"
        android:padding="0dp"
        android:contentDescription="Delete enrollment" />

</LinearLayout>